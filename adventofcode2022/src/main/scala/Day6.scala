class Day6 extends AoCPuzzle {
  override def dayNr: Int = 6
  override def active(): Boolean = true

  private def parseInput() = {
    val rowPatter = "([0-9]+)  ([0-9]+) ([0-9]+)".r
    getData().map {
      row => {
        ""
      }
    }
  }

  override def part1Answer(): String = "???"

  override def part2Answer(): String = "???"
}
